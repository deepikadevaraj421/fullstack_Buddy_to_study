{
  "name": "buddy-to-study",
  "version": "1.0.0",
  "description": "Smart Peer Study Group Matcher",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
